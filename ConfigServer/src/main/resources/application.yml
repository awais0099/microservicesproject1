server:
  port: 8085
  
spring:
  application:
    name: ConfigServer

  cloud:
    config:
      server:
        git:
          uri: https://github.com/awais0099/microservicesproj1-config-server
          clone-on-start: true